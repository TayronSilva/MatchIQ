# URL base da API em produção (inclui o /api).
# Em dev, deixe vazio para usar o proxy do Vite (localhost:8080).
# Exemplo (Render): VITE_API_URL=https://matchiq-backend.onrender.com/api
VITE_API_URL=
